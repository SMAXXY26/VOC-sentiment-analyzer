apiVersion: v1
kind: Service
metadata:
  name: analyzer
  namespace: cx-pipeline
spec:
  selector:
    app: analyzer
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
