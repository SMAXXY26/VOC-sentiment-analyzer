apiVersion: v1
kind: Service
metadata:
  name: producer
  namespace: cx-pipeline
spec:
  selector:
    app: producer
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      nodePort: 30001
    - name: metrics
      port: 9001
      targetPort: 9001
  type: NodePort
